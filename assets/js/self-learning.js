(function(){"use strict";var ctx=null,master=null,musicBus=null,beatTimer=null,nextBeat=0,beat=0,questionTimer=null,submitting=false,retryAction=null;
function node(tag,cls,text){var n=document.createElement(tag);if(cls)n.className=cls;if(text!==undefined)n.textContent=text;return n;}
function initMusic(){try{if(ctx)return;ctx=new(window.AudioContext||window.webkitAudioContext)();master=ctx.createGain();musicBus=ctx.createGain();var limiter=ctx.createDynamicsCompressor();limiter.threshold.value=-5;limiter.knee.value=8;limiter.ratio.value=3;limiter.attack.value=.003;limiter.release.value=.12;master.gain.value=1;musicBus.gain.value=.72;musicBus.connect(master);master.connect(limiter);limiter.connect(ctx.destination);nextBeat=ctx.currentTime+.04;beatTimer=setInterval(schedule,75);}catch(e){ctx=null;}}
function tone(freq,when,length,type,gainValue,bus){if(!ctx)return;var osc=ctx.createOscillator(),gain=ctx.createGain();osc.type=type;osc.frequency.setValueAtTime(freq,when);gain.gain.setValueAtTime(.001,when);gain.gain.exponentialRampToValueAtTime(gainValue,when+.012);gain.gain.exponentialRampToValueAtTime(.001,when+length);osc.connect(gain);gain.connect(bus||master);osc.start(when);osc.stop(when+length+.03);}
function schedule(){if(!ctx||ctx.state!=="running")return;var step=60/136/2,melody=[523.25,659.25,783.99,1046.5,880,698.46,783.99,659.25];while(nextBeat<ctx.currentTime+.2){tone(melody[beat%8],nextBeat,step*.72,"triangle",.34,musicBus);tone([130.81,164.81,146.83,196][Math.floor(beat/2)%4],nextBeat,step*.86,"square",.2,musicBus);tone(1567.98,nextBeat,step*.09,"square",.09,musicBus);if(beat%4===2)tone(98,nextBeat,step*.22,"sawtooth",.14,musicBus);nextBeat+=step;beat++;}}
function startMusic(){initMusic();if(ctx)ctx.resume().catch(function(){});}
function fadeMusic(){if(ctx&&master){master.gain.cancelScheduledValues(ctx.currentTime);master.gain.linearRampToValueAtTime(0,ctx.currentTime+.45);}}
function sfx(kind){if(!ctx)return;var now=ctx.currentTime;if(kind==="correct"){tone(880,now,.13,"sine",.7);tone(1318.5,now+.08,.2,"triangle",.62);}else if(kind==="timeout"){tone(260,now,.16,"square",.58);tone(196,now+.12,.25,"sawtooth",.48);}else{tone(220,now,.16,"sawtooth",.65);tone(174.61,now+.1,.22,"square",.52);}}
function api(body){return fetch(location.pathname+location.search,{method:"POST",headers:{Accept:"application/json","X-Requested-With":"XMLHttpRequest"},credentials:"same-origin",body:body}).then(async function(response){var data;try{data=await response.json();}catch(e){throw new Error("Respons server tidak valid.");}if(!response.ok||!data.success)throw new Error(data.message||"Jawaban belum tersimpan.");return data;});}
function stopTimer(){if(questionTimer){clearInterval(questionTimer);questionTimer=null;}}
function startTimer(form,timer){stopTimer();var left=20,label=timer.querySelector("span"),begin=new FormData();begin.set("reading_ajax","begin");begin.set("session_id",form.elements.session_id.value);begin.set("question_id",form.elements.question_id.value);begin.set("csrf_token",form.elements.csrf_token.value);api(begin).catch(function(){});label.textContent="20";questionTimer=setInterval(function(){left--;label.textContent=String(Math.max(0,left));timer.classList.toggle("warning",left<=10);timer.classList.toggle("danger",left<=5);if(left===5)sfx("timeout");if(left<=0){stopTimer();submit(form,null,true);}},1000);}
function update(data){var score=document.querySelector("[data-score]"),counter=document.querySelector("[data-question-counter]"),bar=document.querySelector("[data-game-progress]");if(score)score.textContent=String(data.score);if(counter)counter.textContent=String(data.completed?20:Math.min(20,data.current_index+1));if(bar)bar.style.width=Math.round(data.current_index/20*100)+"%";}
function feedback(form,data){form.querySelectorAll(".choice").forEach(function(button){if(button.value===data.correct_option_id)button.classList.add("is-correct");if(button.value===data.selected_option_id&&!data.correct)button.classList.add("is-wrong");});var box=document.querySelector("[data-inline-feedback]");if(box){box.replaceChildren();box.hidden=false;box.className="reading-feedback "+(data.correct?"correct":"incorrect");box.append(node("h3","",data.correct?"Correct!":data.result==="timed_out"?"Time’s up":"Not quite"),node("p","",data.explanation),node("small","","Correct answer: "+data.correct_option_text));}sfx(data.correct?"correct":data.result==="timed_out"?"timeout":"wrong");}
function submit(form,optionId,timedOut){if(submitting)return;submitting=true;stopTimer();form.querySelectorAll("button").forEach(function(b){b.disabled=true;});var body=new FormData(form);body.set("reading_ajax","submit");body.set("timed_out",timedOut?"1":"0");if(optionId)body.set("option_id",optionId);else body.delete("option_id");retryAction=function(){submitting=false;submit(form,optionId,timedOut);};api(body).then(function(data){retryAction=null;feedback(form,data);update(data);setTimeout(function(){submitting=false;if(data.completed){fadeMusic();location.assign("/student/self_learning.php?id="+body.get("session_id"));}else render(data.next_state);},1450);}).catch(function(error){submitting=false;var box=document.querySelector("[data-inline-feedback]"),retry=document.querySelector("[data-submit-retry]");if(box){box.hidden=false;box.className="reading-feedback error";box.textContent=error.message;}if(retry){retry.hidden=false;retry.onclick=function(){retry.hidden=true;if(retryAction)retryAction();};}form.querySelectorAll("button").forEach(function(b){b.disabled=false;});});}
function bind(){var form=document.querySelector("[data-reading-answer]"),timer=document.querySelector("[data-reading-timer]");if(!form||!timer)return;form.addEventListener("submit",function(event){event.preventDefault();startMusic();if(event.submitter)submit(form,event.submitter.value,false);});startTimer(form,timer);}
function render(state){var stage=document.querySelector("[data-reading-stage]"),q=state.question;if(!stage||!q)return;stage.replaceChildren();var top=node("div","game-top"),progress=node("div","game-progress"),fill=node("span");fill.dataset.gameProgress="";fill.style.width=Math.round(state.current_index/20*100)+"%";progress.append(fill);var timer=node("div","timer-ring"),timerLabel=node("span","","20");timer.dataset.readingTimer="";timer.append(timerLabel);top.append(progress,timer);var card=node("section","card game-card");card.dataset.questionCard="";if(q.short_context)card.append(node("div","short-context",q.short_context));card.append(node("span","eyebrow",String(q.type||"").replaceAll("_"," ")),node("h1","",q.question));var form=node("form");form.dataset.readingAnswer="";[["csrf_token",window.readingCsrf],["session_id",window.readingSession],["question_id",q.id]].forEach(function(pair){var input=document.createElement("input");input.type="hidden";input.name=pair[0];input.value=pair[1];form.append(input);});var choices=node("div","choices");q.options.forEach(function(option){var button=node("button","choice",option.text);button.type="submit";button.name="option_id";button.value=option.id;choices.append(button);});form.append(choices);var result=node("div","reading-feedback");result.dataset.inlineFeedback="";result.setAttribute("aria-live","polite");result.hidden=true;var retry=node("button","button secondary","Retry submission");retry.type="button";retry.dataset.submitRetry="";retry.hidden=true;card.append(form,result,retry);stage.append(top,card);bind();}
document.addEventListener("DOMContentLoaded",function(){var form=document.querySelector("[data-reading-answer]");if(form){window.readingCsrf=form.elements.csrf_token.value;window.readingSession=form.elements.session_id.value;bind();document.addEventListener("pointerdown",startMusic,{once:true});startMusic();}if(document.body.dataset.sessionComplete==="1"){fadeMusic();var actions=document.querySelector(".result-card .row");if(actions){var choose=node("a","button secondary","Choose Another Level");choose.href="/student/self_learning.php";actions.append(choose);}window.EnglAIVisuals?.confetti();}});})();
