(function(){"use strict";const form=document.querySelector(".wizard-form"),summary=document.getElementById("wizard-summary");if(!form||!summary)return;function update(){const data=new FormData(form),mode=data.get("quiz_mode"),skills=data.getAll("skills[]"),level=data.get("level"),count=data.get("question_count");summary.textContent=`${String(mode).replaceAll("_"," ")} · ${skills.join(", ")||"Final Challenge: all skills"} · ${level} · ${count} questions · server timers enabled`;}form.addEventListener("input",update);update();})();
